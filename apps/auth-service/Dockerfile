# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @glint/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t glint-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system glint-auth-service && \
          adduser --system -G glint-auth-service glint-auth-service

COPY apps/auth-service/dist glint-auth-service/
COPY apps/auth-service/package.json glint-auth-service/
RUN chown -R glint-auth-service:glint-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix glint-auth-service --omit=dev -f install

CMD [ "node", "glint-auth-service" ]
