{
  "name": "@glint/api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@glint/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@glint/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@glint/api-gateway:build:production"
          }
        }
      }
    }
  }
}
